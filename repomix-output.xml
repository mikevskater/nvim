This file is a merged representation of a subset of the codebase, containing specifically included files and files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: lua/myconfig, lua/myconfig/core, lua/myconfig/core/keymaps.lua, lua/myconfig/core/options.lua, lua/myconfig/plugins, lua/myconfig/plugins/autopairs.lua, lua/myconfig/plugins/colorscheme.lua, lua/myconfig/plugins/comment.lua, lua/myconfig/plugins/completion.lua, lua/myconfig/plugins/fugitive.lua, lua/myconfig/plugins/gitsigns.lua, lua/myconfig/plugins/harpoon.lua, lua/myconfig/plugins/lsp.lua, lua/myconfig/plugins/oil.lua, lua/myconfig/plugins/telescope.lua, lua/myconfig/plugins/treesitter.lua, lua/myconfig/plugins/undotree.lua, lua/myconfig/plugins/whichkey.lua, lua/myconfig/standalone, lua/myconfig/standalone/keymaps.lua, lua/myconfig/standalone/options.lua, lua/myconfig/vscode, lua/myconfig/vscode/init.lua, lua/myconfig/vscode/keymaps.lua, lua/myconfig/vscode/plugins.lua, lua/myconfig/init.lua, lua/myconfig/keymaps.lua, lua/myconfig/lazy.lua, lua/myconfig/options.lua, init.lua, lazy-lock.json
- Files matching these patterns are excluded: *.uid, *.import
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
init.lua
lazy-lock.json
lua/myconfig/core/keymaps.lua
lua/myconfig/core/options.lua
lua/myconfig/init.lua
lua/myconfig/keymaps.lua
lua/myconfig/lazy.lua
lua/myconfig/options.lua
lua/myconfig/plugins/autopairs.lua
lua/myconfig/plugins/colorscheme.lua
lua/myconfig/plugins/comment.lua
lua/myconfig/plugins/completion.lua
lua/myconfig/plugins/fugitive.lua
lua/myconfig/plugins/gitsigns.lua
lua/myconfig/plugins/harpoon.lua
lua/myconfig/plugins/lsp.lua
lua/myconfig/plugins/oil.lua
lua/myconfig/plugins/telescope.lua
lua/myconfig/plugins/treesitter.lua
lua/myconfig/plugins/undotree.lua
lua/myconfig/plugins/whichkey.lua
lua/myconfig/standalone/keymaps.lua
lua/myconfig/standalone/options.lua
lua/myconfig/vscode/init.lua
lua/myconfig/vscode/keymaps.lua
lua/myconfig/vscode/plugins.lua
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="init.lua">
-- ~\AppData\Local\nvim\init.lua
-- =============================================================================
-- MULTI-MODE NEOVIM CONFIGURATION
-- =============================================================================
-- Supports both:
--   1. Standalone Neovim (full-featured IDE)
--   2. VSCode-Neovim (minimal, VSCode-compatible)
--
-- The configuration automatically detects the environment and loads
-- the appropriate modules
-- =============================================================================

-- Detect if running inside VSCode
if vim.g.vscode then
  -- ==========================================================================
  -- VSCODE MODE
  -- ==========================================================================
  -- When running inside VSCode, we load a minimal config
  -- VSCode handles: LSP, completion, file explorer, fuzzy finding, etc.
  -- Neovim handles: Modal editing, text objects, motions
  
  require("myconfig.vscode")
  
else
  -- ==========================================================================
  -- STANDALONE MODE
  -- ==========================================================================
  -- Full-featured Neovim configuration with all plugins
  
  require("myconfig")
  
end

-- =============================================================================
-- NOTES
-- =============================================================================
-- Why separate configs?
-- - VSCode provides its own LSP, completion, file explorer, etc.
-- - Many Neovim plugins conflict with VSCode or are redundant
-- - Different keybindings are needed (some keys must go to VSCode)
-- - Performance: VSCode mode should be lightweight
--
-- What's shared?
-- - Basic options (line numbers, indentation, etc.)
-- - Core keymaps (motions, text manipulation)
-- - Compatible plugins (surround, commentary, flash)
--
-- What's different?
-- - Standalone: Full plugin suite (LSP, Telescope, Oil, etc.)
-- - VSCode: Minimal plugins, VSCode-specific keybindings
-- =============================================================================
</file>

<file path="lazy-lock.json">
{
  "Comment.nvim": { "branch": "master", "commit": "e30b7f2008e52442154b66f7c519bfd2f1e32acb" },
  "blink.cmp": { "branch": "main", "commit": "485c03400608cb6534bbf84da8c1c471fc4808c0" },
  "cmp-nvim-lsp": { "branch": "main", "commit": "bd5a7d6db125d4654b50eeae9f5217f24bb22fd3" },
  "friendly-snippets": { "branch": "main", "commit": "572f5660cf05f8cd8834e096d7b4c921ba18e175" },
  "gitsigns.nvim": { "branch": "main", "commit": "1ee5c1fd068c81f9dd06483e639c2aa4587dc197" },
  "harpoon": { "branch": "harpoon2", "commit": "ed1f853847ffd04b2b61c314865665e1dadf22c7" },
  "lazy.nvim": { "branch": "main", "commit": "6c3bda4aca61a13a9c63f1c1d1b16b9d3be90d7a" },
  "mason-lspconfig.nvim": { "branch": "main", "commit": "6bdb14f230de0904229ec367b410fb817e59b072" },
  "mason.nvim": { "branch": "main", "commit": "ad7146aa61dcaeb54fa900144d768f040090bff0" },
  "nvim-autopairs": { "branch": "master", "commit": "23320e75953ac82e559c610bec5a90d9c6dfa743" },
  "nvim-lspconfig": { "branch": "master", "commit": "623bcf08d5f9ff4ee3ce2686fa1f1947a045b1a5" },
  "nvim-treesitter": { "branch": "master", "commit": "42fc28ba918343ebfd5565147a42a26580579482" },
  "nvim-treesitter-textobjects": { "branch": "master", "commit": "71385f191ec06ffc60e80e6b0c9a9d5daed4824c" },
  "nvim-web-devicons": { "branch": "master", "commit": "b8221e42cf7287c4dcde81f232f58d7b947c210d" },
  "oil.nvim": { "branch": "master", "commit": "919e155fdf38e9148cdb5304faaaf53c20d703ea" },
  "plenary.nvim": { "branch": "master", "commit": "b9fd5226c2f76c951fc8ed5923d85e4de065e509" },
  "rose-pine": { "branch": "main", "commit": "72a04c4065345b51b56aed4859ea1d884f734097" },
  "telescope.nvim": { "branch": "0.1.x", "commit": "a0bbec21143c7bc5f8bb02e0005fa0b982edc026" },
  "undotree": { "branch": "master", "commit": "0f1c9816975b5d7f87d5003a19c53c6fd2ff6f7f" },
  "vim-fugitive": { "branch": "master", "commit": "61b51c09b7c9ce04e821f6cf76ea4f6f903e3cf4" },
  "which-key.nvim": { "branch": "main", "commit": "b4177e3eaf15fe5eb8357ebac2286d488be1ed00" }
}
</file>

<file path="lua/myconfig/core/keymaps.lua">
-- ~\AppData\Local\nvim\lua\myconfig\core\keymaps.lua
-- =============================================================================
-- SHARED KEYMAPS - Works in Both Standalone and VSCode
-- =============================================================================
-- These keymaps work well in both environments
-- VSCode-specific keymaps are in vscode/keymaps.lua
-- =============================================================================

local keymap = vim.keymap.set

-- =============================================================================
-- LEADER KEY
-- =============================================================================
vim.g.mapleader = " "
vim.g.maplocalleader = "\\"

-- =============================================================================
-- SHARED EDITING KEYMAPS
-- =============================================================================
-- These work great in both standalone and VSCode

-- Visual mode: stay in visual after indenting
keymap("v", "<", "<gv", { desc = "Indent left and stay in visual" })
keymap("v", ">", ">gv", { desc = "Indent right and stay in visual" })

-- Visual mode: move lines up/down
keymap("v", "J", ":m '>+1<CR>gv=gv", { desc = "Move selection down" })
keymap("v", "K", ":m '<-2<CR>gv=gv", { desc = "Move selection up" })

-- Keep cursor centered when scrolling
keymap("n", "<C-d>", "<C-d>zz", { desc = "Scroll down and center" })
keymap("n", "<C-u>", "<C-u>zz", { desc = "Scroll up and center" })

-- Keep search terms centered
keymap("n", "n", "nzzzv", { desc = "Next search (centered)" })
keymap("n", "N", "Nzzzv", { desc = "Previous search (centered)" })

-- Clipboard operations
keymap({ "n", "v" }, "<leader>d", '"_d', { desc = "Delete without yanking" })
keymap("x", "<leader>p", '"_dP', { desc = "Paste without yanking" })
keymap({ "n", "v" }, "<leader>y", '"+y', { desc = "Yank to system clipboard" })
keymap("n", "<leader>Y", '"+Y', { desc = "Yank line to clipboard" })

-- Disable annoying defaults
keymap("n", "Q", "<nop>", { desc = "Disable Ex mode" })
keymap("n", "<Space>", "<nop>", { desc = "Disable space" })

-- Replace word under cursor
keymap("n", "<leader>s", [[:%s/\<<C-r><C-w>\>/<C-r><C-w>/gI<Left><Left><Left>]], 
  { desc = "Replace word under cursor" })

-- =============================================================================
-- STANDALONE-ONLY KEYMAPS
-- =============================================================================
-- These only work when NOT in VSCode

if not vim.g.vscode then
  -- File operations
  keymap("n", "<leader>w", ":w<CR>", { desc = "Save file" })
  keymap("n", "<leader>q", ":q<CR>", { desc = "Quit" })
  keymap("n", "<leader><leader>", ":source %<CR>", { desc = "Source file" })
  
  -- Window navigation
  keymap("n", "<C-h>", "<C-w>h", { desc = "Move to left split" })
  keymap("n", "<C-j>", "<C-w>j", { desc = "Move to bottom split" })
  keymap("n", "<C-k>", "<C-w>k", { desc = "Move to top split" })
  keymap("n", "<C-l>", "<C-w>l", { desc = "Move to right split" })
  
  -- Resize splits
  keymap("n", "<C-Up>", ":resize +2<CR>", { desc = "Increase height" })
  keymap("n", "<C-Down>", ":resize -2<CR>", { desc = "Decrease height" })
  keymap("n", "<C-Left>", ":vertical resize -2<CR>", { desc = "Decrease width" })
  keymap("n", "<C-Right>", ":vertical resize +2<CR>", { desc = "Increase width" })
  
  -- Buffer navigation
  keymap("n", "<S-h>", ":bprevious<CR>", { desc = "Previous buffer" })
  keymap("n", "<S-l>", ":bnext<CR>", { desc = "Next buffer" })
  keymap("n", "<leader>bd", ":bdelete<CR>", { desc = "Delete buffer" })
  
  -- Terminal mode
  keymap("t", "<Esc><Esc>", "<C-\\><C-n>", { desc = "Exit terminal mode" })
  
  -- File explorer (will be overridden by oil.nvim)
  keymap("n", "<leader>pv", vim.cmd.Ex, { desc = "Open file explorer" })
end

-- =============================================================================
-- VSCODE-SPECIFIC KEYMAPS
-- =============================================================================
-- These are loaded from vscode/keymaps.lua when in VSCode mode
-- They call VSCode commands via the VSCode Neovim extension API
-- =============================================================================
</file>

<file path="lua/myconfig/core/options.lua">
-- ~\AppData\Local\nvim\lua\myconfig\core\options.lua
-- =============================================================================
-- SHARED OPTIONS - Works in Both Standalone and VSCode
-- =============================================================================
-- These options work well in both environments
-- =============================================================================

local opt = vim.opt

-- =============================================================================
-- EDITOR BEHAVIOR (Shared)
-- =============================================================================

-- Indentation
opt.tabstop = 4
opt.softtabstop = 4
opt.shiftwidth = 4
opt.expandtab = true
opt.smartindent = true
opt.breakindent = true

-- Line wrapping
opt.wrap = false

-- Search
opt.ignorecase = true
opt.smartcase = true
opt.incsearch = true

-- Performance
opt.updatetime = 50
opt.timeoutlen = 300

-- Completion
opt.completeopt = "menu,menuone,noselect"

-- Clipboard
opt.clipboard = "unnamedplus"

-- =============================================================================
-- STANDALONE-ONLY OPTIONS
-- =============================================================================
-- These only apply when NOT in VSCode

if not vim.g.vscode then
  -- UI Appearance
  opt.number = true
  opt.relativenumber = true
  opt.signcolumn = "yes"
  opt.cursorline = true
  opt.guicursor = ""
  opt.termguicolors = true
  opt.scrolloff = 8
  opt.sidescrolloff = 8
  opt.showmode = false
  opt.colorcolumn = "80"
  opt.showcmd = true
  opt.cmdheight = 1
  
  -- Splits
  opt.splitright = true
  opt.splitbelow = true
  
  -- Undo/Backup
  opt.swapfile = false
  opt.backup = false
  local undodir = vim.fn.expand("~/.vim/undodir")
  if vim.fn.isdirectory(undodir) == 0 then
    vim.fn.mkdir(undodir, "p")
  end
  opt.undodir = undodir
  opt.undofile = true
  
  -- Search highlighting
  opt.hlsearch = false
  
  -- Mouse
  opt.mouse = "a"
end

-- =============================================================================
-- VSCODE-SPECIFIC OPTIONS
-- =============================================================================

if vim.g.vscode then
  -- In VSCode, we want minimal options
  -- VSCode handles most of the UI
  
  -- Disable some Neovim UI elements
  opt.number = false
  opt.relativenumber = false
  opt.signcolumn = "no"
  
  -- Let VSCode handle search highlighting
  opt.hlsearch = false
end
</file>

<file path="lua/myconfig/init.lua">
-- ~\AppData\Local\nvim\lua\myconfig\init.lua
-- =============================================================================
-- MAIN CONFIGURATION LOADER
-- =============================================================================
-- This file loads all configuration modules in the correct order
-- Think of this as the "table of contents" for your config
-- =============================================================================

-- STEP 1: Set leader keys FIRST before anything else
-- Leader key is like a namespace for custom commands
-- Space is popular because it's easy to reach and not used by default
vim.g.mapleader = " "        -- Main leader (space bar)
vim.g.maplocalleader = "\\"  -- Local leader for filetype-specific mappings

-- STEP 2: Load basic Vim settings
-- These are editor behaviors (line numbers, tabs, search, etc.)
require("myconfig.options")

-- STEP 3: Load keymaps
-- Custom keyboard shortcuts for navigation and editing
require("myconfig.keymaps")

-- STEP 4: Bootstrap and configure lazy.nvim plugin manager
-- This must come last because plugins might depend on options/keymaps
require("myconfig.lazy")

-- =============================================================================
-- That's it! The lazy.nvim setup will automatically load all plugins
-- from the lua/myconfig/plugins/ directory
-- =============================================================================

-- Optional: Add autocommands here if you need them
-- Example: Auto-format on save, highlight yanked text, etc.
-- For now, we'll keep it simple and add these as needed
</file>

<file path="lua/myconfig/keymaps.lua">
-- ~\AppData\Local\nvim\lua\myconfig\keymaps.lua
-- =============================================================================
-- KEYMAPS - Custom Keyboard Shortcuts
-- =============================================================================
-- vim.keymap.set() is the modern Lua way to set keybindings
-- Format: vim.keymap.set(mode, keys, command, options)
-- Modes: "n" = normal, "i" = insert, "v" = visual, "x" = visual block
-- =============================================================================

local keymap = vim.keymap.set  -- Shorthand for convenience

-- =============================================================================
-- LEADER KEY MAPPINGS
-- =============================================================================
-- Leader is set to Space in init.lua
-- These are your "namespace" for custom commands

-- File explorer (using netrw for now, will be replaced by oil.nvim)
keymap("n", "<leader>pv", vim.cmd.Ex, { desc = "Open file explorer" })

-- =============================================================================
-- BETTER DEFAULTS - Quality of Life Improvements
-- =============================================================================

-- Stay in visual mode when indenting
keymap("v", "<", "<gv", { desc = "Indent left and stay in visual mode" })
keymap("v", ">", ">gv", { desc = "Indent right and stay in visual mode" })

-- Move selected lines up/down (ThePrimeagen's favorite)
keymap("v", "J", ":m '>+1<CR>gv=gv", { desc = "Move selection down" })
keymap("v", "K", ":m '<-2<CR>gv=gv", { desc = "Move selection up" })

-- Keep cursor centered when scrolling
keymap("n", "<C-d>", "<C-d>zz", { desc = "Scroll down and center" })
keymap("n", "<C-u>", "<C-u>zz", { desc = "Scroll up and center" })

-- Keep search terms centered
keymap("n", "n", "nzzzv", { desc = "Next search result (centered)" })
keymap("n", "N", "Nzzzv", { desc = "Previous search result (centered)" })

-- =============================================================================
-- CLIPBOARD OPERATIONS
-- =============================================================================

-- Delete without yanking (delete to void register)
keymap({ "n", "v" }, "<leader>d", '"_d', { desc = "Delete without yanking" })

-- Paste over selection without yanking (ThePrimeagen special)
keymap("x", "<leader>p", '"_dP', { desc = "Paste without yanking selection" })

-- Yank to system clipboard
keymap({ "n", "v" }, "<leader>y", '"+y', { desc = "Yank to system clipboard" })
keymap("n", "<leader>Y", '"+Y', { desc = "Yank line to system clipboard" })

-- =============================================================================
-- WINDOW NAVIGATION
-- =============================================================================

-- Navigate between splits
keymap("n", "<C-h>", "<C-w>h", { desc = "Move to left split" })
keymap("n", "<C-j>", "<C-w>j", { desc = "Move to bottom split" })
keymap("n", "<C-k>", "<C-w>k", { desc = "Move to top split" })
keymap("n", "<C-l>", "<C-w>l", { desc = "Move to right split" })

-- Resize splits
keymap("n", "<C-Up>", ":resize +2<CR>", { desc = "Increase split height" })
keymap("n", "<C-Down>", ":resize -2<CR>", { desc = "Decrease split height" })
keymap("n", "<C-Left>", ":vertical resize -2<CR>", { desc = "Decrease split width" })
keymap("n", "<C-Right>", ":vertical resize +2<CR>", { desc = "Increase split width" })

-- =============================================================================
-- BUFFER NAVIGATION
-- =============================================================================

keymap("n", "<S-h>", ":bprevious<CR>", { desc = "Previous buffer" })
keymap("n", "<S-l>", ":bnext<CR>", { desc = "Next buffer" })
keymap("n", "<leader>bd", ":bdelete<CR>", { desc = "Delete buffer" })

-- =============================================================================
-- QUICK ACTIONS
-- =============================================================================

-- Save file
keymap("n", "<leader>w", ":w<CR>", { desc = "Save file" })

-- Quit
keymap("n", "<leader>q", ":q<CR>", { desc = "Quit" })

-- Source current file (reload config)
keymap("n", "<leader><leader>", ":source %<CR>", { desc = "Source current file" })

-- Replace word under cursor (ThePrimeagen style)
keymap("n", "<leader>s", [[:%s/\<<C-r><C-w>\>/<C-r><C-w>/gI<Left><Left><Left>]], 
  { desc = "Replace word under cursor" })

-- Make current file executable
keymap("n", "<leader>x", ":!chmod +x %<CR>", { desc = "Make file executable" })

-- =============================================================================
-- DISABLE ANNOYING DEFAULTS
-- =============================================================================

-- Disable Q (Ex mode - almost never useful)
keymap("n", "Q", "<nop>", { desc = "Disable Ex mode" })

-- Disable Space in normal mode (we use it as leader)
keymap("n", "<Space>", "<nop>", { desc = "Disable space (used as leader)" })

-- =============================================================================
-- TERMINAL MODE
-- =============================================================================

-- Easier escape from terminal mode
keymap("t", "<Esc><Esc>", "<C-\\><C-n>", { desc = "Exit terminal mode" })

-- =============================================================================
-- Notes:
-- - All keymaps have descriptions (shows up in which-key plugin)
-- - These are based on ThePrimeagen's workflow but you should customize them!
-- - To see what a key does: :map <key>
-- - To unmap a key: :unmap <key>
-- =============================================================================
</file>

<file path="lua/myconfig/lazy.lua">
-- ~\AppData\Local\nvim\lua\myconfig\lazy.lua
-- =============================================================================
-- LAZY.NVIM PLUGIN MANAGER SETUP
-- =============================================================================
-- Lazy.nvim is the modern, fast plugin manager for Neovim
-- It automatically lazy-loads plugins for better startup time
-- https://github.com/folke/lazy.nvim
-- =============================================================================

-- =============================================================================
-- STEP 1: Bootstrap lazy.nvim
-- =============================================================================
-- This automatically installs lazy.nvim if it's not already installed
-- This is the "one-time setup" that happens on first run

local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"

-- Check if lazy.nvim is already installed
if not vim.loop.fs_stat(lazypath) then
  -- If not, clone it from GitHub
  print("Installing lazy.nvim...")
  vim.fn.system({
    "git",
    "clone",
    "--filter=blob:none",  -- Don't download unnecessary git history
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable",     -- Use stable release
    lazypath,
  })
  print("lazy.nvim installed! Please restart Neovim.")
end

-- Add lazy.nvim to runtime path so Neovim can find it
vim.opt.rtp:prepend(lazypath)

-- =============================================================================
-- STEP 2: Configure lazy.nvim
-- =============================================================================

require("lazy").setup({
  -- ==========================================================================
  -- PLUGIN SPECIFICATIONS
  -- ==========================================================================
  -- There are two ways to specify plugins:
  --
  -- 1. Direct specification (simple plugins):
  --    { "username/repo" }
  --
  -- 2. Import from separate files (complex plugins):
  --    { import = "myconfig.plugins.telescope" }
  --
  -- We use method #2 for ALL plugins to keep this file clean and modular
  -- Each plugin gets its own file in lua/myconfig/plugins/
  -- ==========================================================================

  -- Import all plugin configurations from lua/myconfig/plugins/
  -- Each file in that directory should return a plugin specification
  { import = "myconfig.plugins.colorscheme" },  -- Theme
  { import = "myconfig.plugins.telescope" },    -- Fuzzy finder
  { import = "myconfig.plugins.treesitter" },   -- Syntax highlighting
  { import = "myconfig.plugins.lsp" },          -- LSP setup
  { import = "myconfig.plugins.completion" },   -- Auto-completion
  { import = "myconfig.plugins.oil" },          -- File explorer
  { import = "myconfig.plugins.harpoon" },      -- Quick file navigation
  { import = "myconfig.plugins.undotree" },     -- Undo history visualizer
  { import = "myconfig.plugins.fugitive" },     -- Git integration
  { import = "myconfig.plugins.gitsigns" },     -- Git decorations
  { import = "myconfig.plugins.comment" },      -- Smart commenting
  { import = "myconfig.plugins.autopairs" },    -- Auto-close brackets
  { import = "myconfig.plugins.whichkey" },     -- Keybinding helper
  
  -- Add more plugins here by creating new files in plugins/ directory
  -- Example: { import = "myconfig.plugins.copilot" },

}, {
  -- ==========================================================================
  -- LAZY.NVIM OPTIONS
  -- ==========================================================================
  -- These control how lazy.nvim itself behaves
  
  -- UI settings
  ui = {
    -- Use rounded borders for plugin manager window
    border = "rounded",
    -- Icons for plugin states (requires Nerd Font)
    icons = {
      cmd = "⌘",
      config = "🛠",
      event = "📅",
      ft = "📂",
      init = "⚙",
      keys = "🗝",
      plugin = "🔌",
      runtime = "💻",
      source = "📄",
      start = "🚀",
      task = "📌",
      lazy = "💤 ",
    },
  },
  
  -- Performance settings
  performance = {
    rtp = {
      -- Disable some rtp plugins you don't use
      disabled_plugins = {
        "gzip",
        "matchit",
        "matchparen",
        "netrwPlugin",  -- We're using oil.nvim instead
        "tarPlugin",
        "tohtml",
        "tutor",
        "zipPlugin",
      },
    },
  },
  
  -- Check for plugin updates on startup (can be slow, disable if needed)
  checker = {
    enabled = true,
    notify = false,  -- Don't notify about updates (check manually with :Lazy)
  },
  
  -- Automatically check for config changes and reload
  change_detection = {
    enabled = true,
    notify = false,  -- Don't notify about config changes
  },
})

-- =============================================================================
-- LAZY.NVIM KEYBINDINGS
-- =============================================================================
-- Open the lazy.nvim UI to manage plugins

vim.keymap.set("n", "<leader>l", ":Lazy<CR>", { desc = "Open Lazy plugin manager" })

-- =============================================================================
-- USEFUL LAZY.NVIM COMMANDS
-- =============================================================================
-- :Lazy            - Open the lazy.nvim UI
-- :Lazy sync       - Install missing plugins and update existing ones
-- :Lazy clean      - Remove unused plugins
-- :Lazy update     - Update all plugins
-- :Lazy restore    - Restore plugins to lockfile state
-- :Lazy profile    - Show startup profiling
-- :Lazy log        - Show recent updates
-- =============================================================================

-- =============================================================================
-- HOW TO ADD A NEW PLUGIN
-- =============================================================================
-- 1. Create a new file: lua/myconfig/plugins/yourplugin.lua
-- 2. In that file, return a plugin specification (see existing plugins for examples)
-- 3. Add: { import = "myconfig.plugins.yourplugin" } to the list above
-- 4. Restart Neovim or run :Lazy sync
-- =============================================================================
</file>

<file path="lua/myconfig/options.lua">
-- ~\AppData\Local\nvim\lua\myconfig\options.lua
-- =============================================================================
-- VIM OPTIONS - Editor Behavior Settings
-- =============================================================================
-- These settings control how Neovim behaves as an editor
-- vim.opt is the modern Lua way to set options (replaces 'set' in Vimscript)
-- =============================================================================

local opt = vim.opt  -- Shorthand for convenience

-- =============================================================================
-- UI APPEARANCE
-- =============================================================================

-- Line numbers
opt.number = true           -- Show absolute line number on current line
opt.relativenumber = true   -- Show relative line numbers (great for motions like "5j")
opt.signcolumn = "yes"      -- Always show sign column (prevents text shifting)

-- Cursor
opt.cursorline = true       -- Highlight the current line
opt.guicursor = ""          -- Keep block cursor in all modes (ThePrimeagen style)

-- Colors
opt.termguicolors = true    -- Enable 24-bit RGB colors (required for modern themes)

-- Scrolling
opt.scrolloff = 8           -- Keep 8 lines visible above/below cursor
opt.sidescrolloff = 8       -- Keep 8 columns visible left/right of cursor

-- Splits
opt.splitright = true       -- Vertical splits go to the right
opt.splitbelow = true       -- Horizontal splits go below

-- Command line
opt.showcmd = true          -- Show command in bottom bar
opt.cmdheight = 1           -- Height of the command bar

-- Visual feedback
opt.showmode = false        -- Don't show mode (we'll use statusline plugin)
opt.colorcolumn = "80"      -- Show column border at 80 characters

-- =============================================================================
-- EDITING BEHAVIOR
-- =============================================================================

-- Indentation
opt.tabstop = 4             -- Number of spaces a <Tab> counts for
opt.softtabstop = 4         -- Number of spaces for <Tab> in insert mode
opt.shiftwidth = 4          -- Number of spaces for each indentation level
opt.expandtab = true        -- Convert tabs to spaces
opt.smartindent = true      -- Smart auto-indenting for new lines
opt.breakindent = true      -- Wrapped lines will be indented

-- Line wrapping
opt.wrap = false            -- Don't wrap long lines (ThePrimeagen preference)

-- Undo/Backup
opt.swapfile = false        -- Don't create swap files
opt.backup = false          -- Don't create backup files
-- Create undo directory if it doesn't exist
local undodir = vim.fn.expand("~/.vim/undodir")
if vim.fn.isdirectory(undodir) == 0 then
  vim.fn.mkdir(undodir, "p")
end
opt.undodir = undodir       -- Store undo files in dedicated directory
opt.undofile = true         -- Enable persistent undo (survives Neovim restart)

-- =============================================================================
-- SEARCH BEHAVIOR
-- =============================================================================

opt.hlsearch = false        -- Don't highlight all search matches (less visual clutter)
opt.incsearch = true        -- Show search matches as you type
opt.ignorecase = true       -- Case-insensitive search...
opt.smartcase = true        -- ...unless search contains uppercase letter

-- =============================================================================
-- PERFORMANCE
-- =============================================================================

opt.updatetime = 50         -- Faster completion (default is 4000ms)
opt.timeoutlen = 300        -- Time to wait for mapped sequence to complete

-- =============================================================================
-- COMPLETION
-- =============================================================================

opt.completeopt = "menu,menuone,noselect"  -- Better completion experience

-- =============================================================================
-- FILE HANDLING
-- =============================================================================

opt.isfname:append("@-@")   -- Consider @ as part of filename

-- =============================================================================
-- CLIPBOARD
-- =============================================================================

-- Use system clipboard (requires clip.exe on Windows, which should be available)
opt.clipboard = "unnamedplus"

-- =============================================================================
-- MOUSE
-- =============================================================================

opt.mouse = "a"             -- Enable mouse support in all modes

-- =============================================================================
-- NETRW (Built-in File Explorer) Settings
-- =============================================================================
-- We'll use oil.nvim instead, but keep these for reference

vim.g.netrw_browse_split = 0    -- Open files in same window
vim.g.netrw_banner = 0           -- Hide banner
vim.g.netrw_winsize = 25         -- Window size

-- =============================================================================
-- Notes:
-- - These are sensible defaults inspired by ThePrimeagen and modern Neovim usage
-- - You can always check what an option does with :help <option-name>
-- - Example: :help number
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/autopairs.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\autopairs.lua
-- =============================================================================
-- AUTO-PAIRS - Auto-close Brackets/Quotes
-- =============================================================================

return {
  "windwp/nvim-autopairs",
  event = "InsertEnter",
  opts = {},
}

-- Automatically closes (, [, {, ', ", etc.
</file>

<file path="lua/myconfig/plugins/colorscheme.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\colorscheme.lua
-- =============================================================================
-- COLORSCHEME - Rose Pine Theme
-- =============================================================================
-- Why Rose Pine? Clean, easy on the eyes, has dark/light variants
-- ThePrimeagen uses this one
-- Alternatives: tokyonight, catppuccin, gruvbox, onedark
-- =============================================================================

return {
  "rose-pine/neovim",
  name = "rose-pine",
  
  -- Load this plugin immediately (before other plugins)
  -- Colorschemes should load first to avoid flashing
  priority = 1000,
  
  -- Configuration function runs when plugin is loaded
  config = function()
    require("rose-pine").setup({
      -- Variant: "main" (default), "moon", "dawn" (light mode)
      variant = "main",
      
      -- Disable italics if you don't like them
      disable_italics = false,
      
      -- Make background transparent (shows terminal background)
      -- Uncomment if you want transparency
      -- disable_background = true,
      
      -- Custom colors and highlights
      highlight_groups = {
        -- Make the background transparent
        -- Uncomment these if you want a transparent background
        Normal = { bg = "none" },
        NormalFloat = { bg = "none" },
        
        -- Or customize specific highlight groups
        -- CursorLine = { bg = "#2a2837" },
      },
    })
    
    -- Apply the colorscheme
    vim.cmd("colorscheme rose-pine")
  end,
}

-- =============================================================================
-- COLORSCHEME NOTES
-- =============================================================================
-- To change colorscheme:
-- 1. Change the plugin spec above (e.g., "folke/tokyonight.nvim")
-- 2. Update the config function to use the new colorscheme
-- 3. Run :Lazy sync to install
-- 4. Restart Neovim
--
-- Popular alternatives:
-- - folke/tokyonight.nvim (modern, clean)
-- - catppuccin/nvim (pastel, many variants)
-- - ellisonleao/gruvbox.nvim (retro, warm)
-- - navarasu/onedark.nvim (VS Code style)
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/comment.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\comment.lua
-- =============================================================================
-- COMMENT.NVIM - Smart Commenting
-- =============================================================================

return {
  "numToStr/Comment.nvim",
  opts = {},
}

-- Usage: gcc to comment line, gc in visual mode to comment selection
</file>

<file path="lua/myconfig/plugins/completion.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\completion.lua
-- =============================================================================
-- COMPLETION - Auto-completion Engine
-- =============================================================================
-- Why blink.cmp? Much faster than nvim-cmp (0.5-4ms vs 60ms+)
-- Modern, batteries-included, works out of the box
-- https://github.com/Saghen/blink.cmp
-- =============================================================================

return {
  "saghen/blink.cmp",
  
  -- Use a release tag to download pre-built binaries (faster)
  --version = "v0.*",
  
  -- Dependencies
  dependencies = {
    "rafamadriz/friendly-snippets",  -- Collection of snippets
  },
  
  -- Configuration
  opts = {
    -- ==========================================================================
    -- KEYMAP PRESET
    -- ==========================================================================
    -- 'default': <Tab> to select, <Enter> to accept
    -- 'super-tab': <Tab> for everything
    -- 'enter': <Enter> to accept, <Tab> for snippets
    keymap = { 
      preset = "default",
      
      -- Custom keymaps (optional)
      -- Uncomment to customize
      -- ["<Tab>"] = { "select_next", "fallback" },
      -- ["<S-Tab>"] = { "select_prev", "fallback" },
      -- ["<CR>"] = { "accept", "fallback" },
    },
    
    -- ==========================================================================
    -- APPEARANCE
    -- ==========================================================================
    appearance = {
      -- Use nvim-cmp's highlight groups (for theme compatibility)
      use_nvim_cmp_as_default = true,
      
      -- Nerd Font variant: 'mono' or 'normal'
      nerd_font_variant = "mono",
    },
    
    -- ==========================================================================
    -- COMPLETION SOURCES
    -- ==========================================================================
    -- Where completion suggestions come from
    sources = {
      default = { 
        "lsp",       -- From language servers
        "path",      -- File paths
        "snippets",  -- Code snippets
        "buffer",    -- Words from open buffers
      },
      
      -- Per-filetype sources (optional)
      -- providers = {
      --   lsp = { fallback_for = { "lazydev" } },
      -- },
    },
    
    -- ==========================================================================
    -- COMPLETION BEHAVIOR
    -- ==========================================================================
    completion = {
      -- Automatically show completion menu
      menu = {
        auto_show = true,
        
        -- Draw behavior
        draw = {
          -- Show treesitter context
          treesitter = { "lsp" },
          
          -- Columns to display
          columns = { 
            { "kind_icon" }, 
            { "label", "label_description", gap = 1 },
          },
        },
      },
      
      -- Documentation window
      documentation = {
        auto_show = true,
        auto_show_delay_ms = 200,
      },
      
      -- Ghost text (inline suggestions)
      ghost_text = {
        enabled = false,  -- Disable by default (can be distracting)
      },
      
      -- Auto-brackets
      accept = {
        auto_brackets = {
          enabled = true,  -- Auto-insert closing brackets
        },
      },
    },
    
    -- ==========================================================================
    -- SIGNATURE HELP
    -- ==========================================================================
    -- Show function signatures while typing
    signature = { 
      enabled = true 
    },
  },
  
  -- ==========================================================================
  -- ADDITIONAL SETUP
  -- ==========================================================================
  config = function(_, opts)
    require("blink.cmp").setup(opts)
  end,
}

-- =============================================================================
-- COMPLETION NOTES
-- =============================================================================
-- blink.cmp comes with sensible defaults, minimal config needed!
--
-- Default keybindings:
-- <Tab>      - Select next item
-- <S-Tab>    - Select previous item
-- <CR>       - Accept completion
-- <C-e>      - Close completion menu
-- <C-space>  - Trigger completion manually
--
-- Completion sources explained:
-- - lsp: From language servers (functions, variables, etc.)
-- - path: File and directory paths
-- - buffer: Words from currently open files
-- - snippets: Code templates
--
-- Performance notes:
-- - blink.cmp is 10-100x faster than nvim-cmp
-- - Updates on every keystroke with minimal lag
-- - Uses Rust for fuzzy matching (compiled to WASM)
--
-- Troubleshooting:
-- 1. No completions? Check LSP is running: :LspInfo
-- 2. Check blink.cmp is loaded: :Lazy
-- 3. Manually trigger: <C-space>
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/fugitive.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\fugitive.lua
-- =============================================================================
-- VIM-FUGITIVE - Git Integration
-- =============================================================================
-- The best Git plugin for Vim/Neovim by tpope
-- =============================================================================

return {
  "tpope/vim-fugitive",
  
  config = function()
    vim.keymap.set("n", "<leader>gs", vim.cmd.Git,
      { desc = "Git status" })
  end,
}

-- Usage: <leader>gs for git status
-- Then: s to stage, u to unstage, cc to commit
</file>

<file path="lua/myconfig/plugins/gitsigns.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\gitsigns.lua
-- =============================================================================
-- GITSIGNS - Git Decorations
-- =============================================================================
-- Shows git changes in the sign column (added/modified/deleted lines)
-- =============================================================================

return {
  "lewis6991/gitsigns.nvim",
  
  opts = {
    signs = {
      add          = { text = '│' },
      change       = { text = '│' },
      delete       = { text = '_' },
      topdelete    = { text = '‾' },
      changedelete = { text = '~' },
      untracked    = { text = '┆' },
    },
    on_attach = function(bufnr)
      local gs = package.loaded.gitsigns
      local keymap = vim.keymap.set
      
      -- Navigation
      keymap('n', ']c', gs.next_hunk, { buffer = bufnr, desc = "Next git hunk" })
      keymap('n', '[c', gs.prev_hunk, { buffer = bufnr, desc = "Previous git hunk" })
      
      -- Actions
      keymap('n', '<leader>hs', gs.stage_hunk, { buffer = bufnr, desc = "Stage hunk" })
      keymap('n', '<leader>hr', gs.reset_hunk, { buffer = bufnr, desc = "Reset hunk" })
      keymap('n', '<leader>hp', gs.preview_hunk, { buffer = bufnr, desc = "Preview hunk" })
      keymap('n', '<leader>hb', gs.blame_line, { buffer = bufnr, desc = "Blame line" })
    end,
  },
}
</file>

<file path="lua/myconfig/plugins/harpoon.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\harpoon.lua
-- =============================================================================
-- HARPOON - Quick File Navigation
-- =============================================================================
-- Why Harpoon? ThePrimeagen's plugin for marking and jumping to files
-- Perfect for working with 4-5 main files in a project
-- =============================================================================

return {
  "theprimeagen/harpoon",
  branch = "harpoon2",
  dependencies = { "nvim-lua/plenary.nvim" },
  
  config = function()
    local harpoon = require("harpoon")
    harpoon:setup()
    
    -- Keybindings
    vim.keymap.set("n", "<leader>a", function() harpoon:list():add() end,
      { desc = "Harpoon: Add file" })
    vim.keymap.set("n", "<C-e>", function() harpoon.ui:toggle_quick_menu(harpoon:list()) end,
      { desc = "Harpoon: Toggle menu" })
    
    -- Quick jump to files 1-4
    vim.keymap.set("n", "<C-h>", function() harpoon:list():select(1) end,
      { desc = "Harpoon: File 1" })
    vim.keymap.set("n", "<C-t>", function() harpoon:list():select(2) end,
      { desc = "Harpoon: File 2" })
    vim.keymap.set("n", "<C-n>", function() harpoon:list():select(3) end,
      { desc = "Harpoon: File 3" })
    vim.keymap.set("n", "<C-s>", function() harpoon:list():select(4) end,
      { desc = "Harpoon: File 4" })
  end,
}

-- =============================================================================
-- HARPOON USAGE
-- =============================================================================
-- <leader>a  - Mark current file
-- <C-e>      - Open harpoon menu
-- <C-h/t/n/s> - Jump to marked files 1-4
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/lsp.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\lsp.lua
-- =============================================================================
-- LSP - Language Server Protocol
-- =============================================================================
-- Why LSP? Provides IDE features: code completion, go-to-definition, 
-- diagnostics, etc. Mason manages LSP server installation.
-- =============================================================================

return {
  -- Mason: Package manager for LSP servers, formatters, linters
  {
    "williamboman/mason.nvim",
    config = function()
      require("mason").setup({
        ui = {
          border = "rounded",
          icons = {
            package_installed = "✓",
            package_pending = "➜",
            package_uninstalled = "✗",
          },
        },
      })
    end,
  },
  
  -- Mason-LSPConfig: Bridges mason.nvim and lspconfig
  {
    "williamboman/mason-lspconfig.nvim",
    dependencies = {
      "williamboman/mason.nvim",
    },
    config = function()
      require("mason-lspconfig").setup({
        -- Auto-install these language servers
        -- Add/remove based on your needs
        ensure_installed = {
          "lua_ls",       -- Lua (for Neovim config)
          "ts_ls",        -- TypeScript/JavaScript
          "html",         -- HTML
          "cssls",        -- CSS
          "jsonls",       -- JSON
          "pyright",      -- Python
          -- Add more as needed:
          -- "rust_analyzer", -- Rust
          -- "gopls",         -- Go
          -- "clangd",        -- C/C++
        },
        
        -- Auto-install when entering a file with that filetype
        automatic_installation = true,
      })
    end,
  },
  
  -- LSPConfig: Configures Neovim's built-in LSP client
  {
    "neovim/nvim-lspconfig",
    dependencies = {
      "williamboman/mason-lspconfig.nvim",
      "hrsh7th/cmp-nvim-lsp",  -- LSP completion source
    },
    config = function()
      local lspconfig = require("lspconfig")
      local cmp_nvim_lsp = require("cmp_nvim_lsp")
      
      -- ==========================================================================
      -- LSP KEYBINDINGS (when LSP attaches to buffer)
      -- ==========================================================================
      vim.api.nvim_create_autocmd("LspAttach", {
        group = vim.api.nvim_create_augroup("UserLspConfig", {}),
        callback = function(ev)
          local opts = { buffer = ev.buf, silent = true }
          local keymap = vim.keymap.set
          
          -- Navigation
          keymap("n", "gd", vim.lsp.buf.definition, 
            vim.tbl_extend("force", opts, { desc = "Go to definition" }))
          keymap("n", "gD", vim.lsp.buf.declaration, 
            vim.tbl_extend("force", opts, { desc = "Go to declaration" }))
          keymap("n", "gi", vim.lsp.buf.implementation, 
            vim.tbl_extend("force", opts, { desc = "Go to implementation" }))
          keymap("n", "gr", vim.lsp.buf.references, 
            vim.tbl_extend("force", opts, { desc = "Show references" }))
          
          -- Information
          keymap("n", "K", vim.lsp.buf.hover, 
            vim.tbl_extend("force", opts, { desc = "Show hover information" }))
          keymap("n", "<C-k>", vim.lsp.buf.signature_help, 
            vim.tbl_extend("force", opts, { desc = "Show signature help" }))
          
          -- Diagnostics
          keymap("n", "[d", vim.diagnostic.goto_prev, 
            vim.tbl_extend("force", opts, { desc = "Previous diagnostic" }))
          keymap("n", "]d", vim.diagnostic.goto_next, 
            vim.tbl_extend("force", opts, { desc = "Next diagnostic" }))
          keymap("n", "<leader>e", vim.diagnostic.open_float, 
            vim.tbl_extend("force", opts, { desc = "Show diagnostic" }))
          keymap("n", "<leader>dl", vim.diagnostic.setloclist, 
            vim.tbl_extend("force", opts, { desc = "Diagnostic loclist" }))
          
          -- Actions
          keymap({ "n", "v" }, "<leader>ca", vim.lsp.buf.code_action, 
            vim.tbl_extend("force", opts, { desc = "Code action" }))
          keymap("n", "<leader>rn", vim.lsp.buf.rename, 
            vim.tbl_extend("force", opts, { desc = "Rename symbol" }))
          keymap("n", "<leader>fm", function()
            vim.lsp.buf.format({ async = true })
          end, vim.tbl_extend("force", opts, { desc = "Format buffer" }))
          
          -- Workspace
          keymap("n", "<leader>wa", vim.lsp.buf.add_workspace_folder, 
            vim.tbl_extend("force", opts, { desc = "Add workspace folder" }))
          keymap("n", "<leader>wr", vim.lsp.buf.remove_workspace_folder, 
            vim.tbl_extend("force", opts, { desc = "Remove workspace folder" }))
          keymap("n", "<leader>wl", function()
            print(vim.inspect(vim.lsp.buf.list_workspace_folders()))
          end, vim.tbl_extend("force", opts, { desc = "List workspace folders" }))
        end,
      })
      
      -- ==========================================================================
      -- LSP CAPABILITIES (what LSP client can do)
      -- ==========================================================================
      local capabilities = cmp_nvim_lsp.default_capabilities()
      
      -- ==========================================================================
      -- CONFIGURE SPECIFIC LANGUAGE SERVERS
      -- ==========================================================================
      
      -- Lua (with Neovim-specific settings)
      lspconfig.lua_ls.setup({
        capabilities = capabilities,
        settings = {
          Lua = {
            diagnostics = {
              globals = { "vim" },  -- Recognize 'vim' global
            },
            workspace = {
              library = {
                [vim.fn.expand("$VIMRUNTIME/lua")] = true,
                [vim.fn.stdpath("config") .. "/lua"] = true,
              },
            },
          },
        },
      })
      
      -- TypeScript/JavaScript
      lspconfig.ts_ls.setup({
        capabilities = capabilities,
      })
      
      -- Python
      lspconfig.pyright.setup({
        capabilities = capabilities,
      })
      
      -- HTML
      lspconfig.html.setup({
        capabilities = capabilities,
      })
      
      -- CSS
      lspconfig.cssls.setup({
        capabilities = capabilities,
      })
      
      -- JSON
      lspconfig.jsonls.setup({
        capabilities = capabilities,
      })
      
      -- Add more language servers here following the same pattern
      -- Check available servers: https://github.com/neovim/nvim-lspconfig/blob/master/doc/configs.md
    end,
  },
}

-- =============================================================================
-- LSP NOTES
-- =============================================================================
-- Mason UI: :Mason
-- Install LSP: :MasonInstall <server-name>
-- LSP Info: :LspInfo
-- LSP Log: :LspLog
--
-- Common LSP keybindings (defined above):
-- gd       - Go to definition
-- gr       - Show references
-- K        - Hover documentation
-- <leader>ca - Code actions
-- <leader>rn - Rename
-- <leader>fm - Format
-- [d / ]d  - Navigate diagnostics
--
-- Troubleshooting:
-- 1. Check if LSP is running: :LspInfo
-- 2. Check logs: :LspLog
-- 3. Restart LSP: :LspRestart
-- 4. Make sure language server is installed: :Mason
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/oil.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\oil.lua
-- =============================================================================
-- OIL.NVIM - File Explorer as Buffer
-- =============================================================================
-- Why oil.nvim? Edit your filesystem like a normal buffer
-- Use Vim motions to move/rename/delete files
-- Much more intuitive than traditional file trees
-- https://github.com/stevearc/oil.nvim
-- =============================================================================

return {
  "stevearc/oil.nvim",
  
  -- Dependencies (for file icons)
  dependencies = { 
    "nvim-tree/nvim-web-devicons" 
  },
  
  -- Don't lazy load (needed for directory navigation)
  lazy = false,
  
  -- Configuration
  opts = {
    -- ==========================================================================
    -- DEFAULT FILE EXPLORER
    -- ==========================================================================
    -- Replace netrw (Vim's built-in file explorer)
    default_file_explorer = true,
    
    -- ==========================================================================
    -- COLUMNS TO DISPLAY
    -- ==========================================================================
    -- What information to show for each file
    columns = {
      "icon",     -- File type icon
      -- "permissions", -- Unix permissions (uncomment if needed)
      -- "size",        -- File size (uncomment if needed)
      -- "mtime",       -- Modified time (uncomment if needed)
    },
    
    -- ==========================================================================
    -- BUFFER OPTIONS
    -- ==========================================================================
    -- How the oil buffer behaves
    buf_options = {
      buflisted = false,  -- Don't show in buffer list
      bufhidden = "hide", -- Hide when abandoned
    },
    
    -- ==========================================================================
    -- WINDOW OPTIONS
    -- ==========================================================================
    -- Split behavior when opening oil
    win_options = {
      wrap = false,
      signcolumn = "no",
      cursorcolumn = false,
      foldcolumn = "0",
      spell = false,
      list = false,
      conceallevel = 3,
      concealcursor = "nvic",
    },
    
    -- ==========================================================================
    -- DELETE TO TRASH (safer than permanent delete)
    -- ==========================================================================
    delete_to_trash = true,  -- Send to recycle bin instead of permanent delete
    
    -- ==========================================================================
    -- SKIP CONFIRMATION FOR SIMPLE OPERATIONS
    -- ==========================================================================
    skip_confirm_for_simple_edits = false,  -- Always confirm changes
    
    -- ==========================================================================
    -- PROMPT TO SAVE CHANGES
    -- ==========================================================================
    prompt_save_on_select_new_entry = true,  -- Save before navigating away
    
    -- ==========================================================================
    -- CLEANUP AFTER BUFFER CLOSE
    -- ==========================================================================
    cleanup_delay_ms = 2000,  -- Clean up hidden buffers after 2 seconds
    
    -- ==========================================================================
    -- LSP FILE OPERATIONS
    -- ==========================================================================
    -- Experimental: Notify LSP of file operations (rename, move, etc.)
    lsp_file_methods = {
      timeout_ms = 1000,
      autosave_changes = false,
    },
    
    -- ==========================================================================
    -- VIEW OPTIONS
    -- ==========================================================================
    view_options = {
      show_hidden = true,      -- Show hidden files (dotfiles)
      is_hidden_file = function(name, bufnr)
        return vim.startswith(name, ".")  -- Files starting with '.'
      end,
      is_always_hidden = function(name, bufnr)
        return false  -- No files are permanently hidden
      end,
      sort = {
        { "type", "asc" },   -- Directories first
        { "name", "asc" },   -- Then alphabetically
      },
    },
    
    -- ==========================================================================
    -- KEYMAPS (within oil buffer)
    -- ==========================================================================
    keymaps = {
      ["g?"] = "actions.show_help",
      ["<CR>"] = "actions.select",
      ["<C-s>"] = "actions.select_vsplit",
      ["<C-h>"] = "actions.select_split",
      ["<C-t>"] = "actions.select_tab",
      ["<C-p>"] = "actions.preview",
      ["<C-c>"] = "actions.close",
      ["<C-l>"] = "actions.refresh",
      ["-"] = "actions.parent",
      ["_"] = "actions.open_cwd",
      ["`"] = "actions.cd",
      ["~"] = "actions.tcd",
      ["gs"] = "actions.change_sort",
      ["gx"] = "actions.open_external",
      ["g."] = "actions.toggle_hidden",
      ["g\\"] = "actions.toggle_trash",
    },
    
    -- ==========================================================================
    -- USE DEFAULT KEYMAPS
    -- ==========================================================================
    use_default_keymaps = true,
  },
  
  -- Additional setup after options
  config = function(_, opts)
    require("oil").setup(opts)
    
    -- ==========================================================================
    -- CUSTOM KEYBINDINGS
    -- ==========================================================================
    -- Open oil in current directory
    vim.keymap.set("n", "-", "<CMD>Oil<CR>", 
      { desc = "Open parent directory" })
    
    -- Open oil in float window
    vim.keymap.set("n", "<leader>-", "<CMD>Oil --float<CR>", 
      { desc = "Open parent directory (float)" })
  end,
}

-- =============================================================================
-- OIL.NVIM USAGE GUIDE
-- =============================================================================
-- Open oil: Press '-' (opens parent directory)
--
-- Within oil buffer:
-- <CR>     - Open file/directory
-- -        - Go to parent directory
-- _        - Go to current working directory
-- g?       - Show help
-- g.       - Toggle hidden files
-- d        - Delete file (sends to trash)
-- yy       - Copy file path
-- p        - Paste (move/copy file)
--
-- File operations (like in normal buffer):
-- dd       - Cut line (stages file for move/delete)
-- yy       - Yank line (copy file path)
-- p        - Paste
-- R        - Rename (just edit the filename!)
-- :w       - Save changes (performs file operations)
--
-- Example workflow:
-- 1. Press '-' to open oil
-- 2. Navigate with j/k
-- 3. Press 'R' on a file to rename it (edit the text)
-- 4. Press ':w' to save (actually renames the file)
--
-- This is MUCH more intuitive than traditional file explorers!
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/telescope.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\telescope.lua
-- =============================================================================
-- TELESCOPE - Fuzzy Finder
-- =============================================================================
-- Why Telescope? THE fuzzy finder for Neovim
-- Find files, search text, browse git commits, and more
-- https://github.com/nvim-telescope/telescope.nvim
-- =============================================================================

return {
  "nvim-telescope/telescope.nvim",
  branch = "0.1.x",
  
  -- Dependencies (other plugins this plugin needs)
  dependencies = {
    "nvim-lua/plenary.nvim",  -- Required utility functions
  },
  
  -- Configuration
  config = function()
    local telescope = require("telescope")
    local actions = require("telescope.actions")
    
    telescope.setup({
      defaults = {
        -- Layout
        layout_strategy = "horizontal",
        layout_config = {
          horizontal = {
            preview_width = 0.55,
            results_width = 0.8,
          },
          width = 0.87,
          height = 0.80,
          preview_cutoff = 120,
        },
        
        -- Behavior
        prompt_prefix = " 🔍 ",
        selection_caret = "  ",
        path_display = { "truncate" },  -- Shorten long paths
        
        -- Keymaps within Telescope
        mappings = {
          i = {  -- Insert mode
            ["<C-k>"] = actions.move_selection_previous,
            ["<C-j>"] = actions.move_selection_next,
            ["<C-q>"] = actions.send_selected_to_qflist + actions.open_qflist,
          },
        },
      },
      
      pickers = {
        -- Customize individual pickers
        find_files = {
          hidden = true,  -- Show hidden files
          -- Follow symbolic links
          follow = true,
        },
      },
    })
    
    -- ==========================================================================
    -- TELESCOPE KEYBINDINGS
    -- ==========================================================================
    local builtin = require("telescope.builtin")
    local keymap = vim.keymap.set
    
    -- File pickers
    keymap("n", "<leader>ff", builtin.find_files, 
      { desc = "Find files" })
    keymap("n", "<leader>fg", builtin.git_files, 
      { desc = "Find git files" })
    keymap("n", "<leader>fr", builtin.oldfiles, 
      { desc = "Find recent files" })
    
    -- Search pickers
    keymap("n", "<leader>fw", builtin.live_grep, 
      { desc = "Find word (grep)" })
    keymap("n", "<leader>fs", builtin.grep_string, 
      { desc = "Find string under cursor" })
    
    -- Buffer pickers
    keymap("n", "<leader>fb", builtin.buffers, 
      { desc = "Find buffers" })
    
    -- Help pickers
    keymap("n", "<leader>fh", builtin.help_tags, 
      { desc = "Find help" })
    keymap("n", "<leader>fk", builtin.keymaps, 
      { desc = "Find keymaps" })
    
    -- Git pickers
    keymap("n", "<leader>fc", builtin.git_commits, 
      { desc = "Find git commits" })
    keymap("n", "<leader>ft", builtin.git_status, 
      { desc = "Find git status" })
  end,
}

-- =============================================================================
-- TELESCOPE NOTES
-- =============================================================================
-- Requirements:
-- - ripgrep (for live_grep) - Install: winget install BurntSushi.ripgrep.MSVC
-- - fd (optional, for faster file finding) - Install: winget install sharkdp.fd
--
-- Useful commands:
-- :Telescope              - Show all pickers
-- :Telescope find_files   - Find files
-- :Telescope live_grep    - Search in files
-- :Telescope help_tags    - Search help
--
-- In Telescope window:
-- <C-j/k>    - Move up/down
-- <CR>       - Open file
-- <C-x>      - Open in horizontal split
-- <C-v>      - Open in vertical split
-- <C-t>      - Open in new tab
-- <C-q>      - Send to quickfix list
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/treesitter.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\treesitter.lua
-- =============================================================================
-- TREESITTER - Advanced Syntax Highlighting
-- =============================================================================
-- Why Treesitter? Better syntax highlighting than regex-based highlighting
-- Understands code structure, enables smart text objects, code folding, etc.
-- https://github.com/nvim-treesitter/nvim-treesitter
-- =============================================================================

return {
  "nvim-treesitter/nvim-treesitter",
  
  -- Run :TSUpdate after installation/updates
  build = ":TSUpdate",
  
  -- Load on events for better startup time
  event = { "BufReadPre", "BufNewFile" },
  
  -- Optional dependencies (useful text objects)
  dependencies = {
    "nvim-treesitter/nvim-treesitter-textobjects",
  },
  
  config = function()
    require("nvim-treesitter.configs").setup({
      -- ==========================================================================
      -- PARSER INSTALLATION
      -- ==========================================================================
      -- Install parsers for these languages automatically
      -- Add/remove languages as needed
      ensure_installed = {
        -- General purpose
        "lua",        -- Neovim config
        "vim",        -- Vim help files
        "vimdoc",     -- Vim documentation
        
        -- Web development
        "javascript",
        "typescript",
        "tsx",
        "html",
        "css",
        "json",
        
        -- Systems programming
        "c",
        "cpp",
        "rust",
        "go",
        
        -- Scripting
        "python",
        "bash",
        
        -- Markup/config
        "markdown",
        "markdown_inline",
        "yaml",
        "toml",
        
        -- Add more as needed...
      },
      
      -- Install parsers synchronously (only for ensure_installed)
      sync_install = false,
      
      -- Automatically install missing parsers when entering buffer
      auto_install = true,
      
      -- ==========================================================================
      -- HIGHLIGHTING
      -- ==========================================================================
      highlight = {
        enable = true,
        
        -- Disable for large files (improves performance)
        disable = function(lang, buf)
          local max_filesize = 100 * 1024 -- 100 KB
          local ok, stats = pcall(vim.loop.fs_stat, vim.api.nvim_buf_get_name(buf))
          if ok and stats and stats.size > max_filesize then
            return true
          end
        end,
        
        -- Enable additional highlighting by vim's regex
        additional_vim_regex_highlighting = false,
      },
      
      -- ==========================================================================
      -- INDENTATION
      -- ==========================================================================
      indent = {
        enable = true,
      },
      
      -- ==========================================================================
      -- INCREMENTAL SELECTION
      -- ==========================================================================
      -- Expand selection based on syntax tree
      incremental_selection = {
        enable = true,
        keymaps = {
          init_selection = "<C-space>",
          node_incremental = "<C-space>",
          scope_incremental = false,
          node_decremental = "<bs>",
        },
      },
      
      -- ==========================================================================
      -- TEXT OBJECTS (requires nvim-treesitter-textobjects)
      -- ==========================================================================
      -- Smart text objects based on syntax tree
      -- Example: "daf" = delete a function, "vac" = select a class
      textobjects = {
        select = {
          enable = true,
          lookahead = true,  -- Automatically jump forward to textobj
          keymaps = {
            -- Functions
            ["af"] = "@function.outer",
            ["if"] = "@function.inner",
            
            -- Classes
            ["ac"] = "@class.outer",
            ["ic"] = "@class.inner",
            
            -- Loops
            ["al"] = "@loop.outer",
            ["il"] = "@loop.inner",
            
            -- Conditionals
            ["ai"] = "@conditional.outer",
            ["ii"] = "@conditional.inner",
            
            -- Parameters/arguments
            ["aa"] = "@parameter.outer",
            ["ia"] = "@parameter.inner",
          },
        },
        
        -- Move between functions, classes, etc.
        move = {
          enable = true,
          set_jumps = true,  -- Add to jumplist
          goto_next_start = {
            ["]f"] = "@function.outer",
            ["]c"] = "@class.outer",
          },
          goto_next_end = {
            ["]F"] = "@function.outer",
            ["]C"] = "@class.outer",
          },
          goto_previous_start = {
            ["[f"] = "@function.outer",
            ["[c"] = "@class.outer",
          },
          goto_previous_end = {
            ["[F"] = "@function.outer",
            ["[C"] = "@class.outer",
          },
        },
      },
    })
  end,
}

-- =============================================================================
-- TREESITTER NOTES
-- =============================================================================
-- Treesitter requires a C compiler on Windows:
-- 1. Install zig: winget install zig.zig
-- 2. OR install Visual Studio Build Tools
--
-- Useful commands:
-- :TSInstall <language>    - Install parser for language
-- :TSUpdate                - Update all parsers
-- :TSUninstall <language>  - Remove parser
-- :TSInstallInfo           - Show installed parsers
--
-- Text object examples (with textobjects enabled):
-- daf  - Delete a function
-- vif  - Select inside function
-- yac  - Yank a class
-- ]f   - Jump to next function
-- [f   - Jump to previous function
-- =============================================================================
</file>

<file path="lua/myconfig/plugins/undotree.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\undotree.lua
-- =============================================================================
-- UNDOTREE - Visualize Undo History
-- =============================================================================
-- Shows undo history as a tree, travel through time in your file
-- =============================================================================

return {
  "mbbill/undotree",
  
  config = function()
    vim.keymap.set("n", "<leader>u", vim.cmd.UndotreeToggle,
      { desc = "Toggle undotree" })
  end,
}

-- Usage: <leader>u to toggle undotree panel
</file>

<file path="lua/myconfig/plugins/whichkey.lua">
-- ~\AppData\Local\nvim\lua\myconfig\plugins\whichkey.lua
-- =============================================================================
-- WHICH-KEY - Keybinding Helper
-- =============================================================================
-- Shows available keybindings in a popup as you type
-- Perfect for learning your own keymaps!
-- =============================================================================

return {
  "folke/which-key.nvim",
  event = "VeryLazy",
  
  opts = {
    plugins = {
      marks = true,
      registers = true,
      spelling = {
        enabled = true,
        suggestions = 20,
      },
    },
    
    -- Add custom key group names
    spec = {
      { "<leader>f", group = "Find (Telescope)" },
      { "<leader>g", group = "Git" },
      { "<leader>h", group = "Git Hunks" },
      { "<leader>w", group = "Workspace" },
      { "<leader>d", group = "Diagnostics" },
      { "<leader>c", group = "Code" },
      { "<leader>b", group = "Buffer" },
    },
  },
}

-- =============================================================================
-- WHICH-KEY USAGE
-- =============================================================================
-- Just start typing a key sequence (like <leader>) and wait a moment
-- A popup will show all available keybindings!
-- =============================================================================
</file>

<file path="lua/myconfig/standalone/keymaps.lua">
-- ~\AppData\Local\nvim\lua\myconfig\standalone\keymaps.lua
-- =============================================================================
-- STANDALONE-ONLY KEYMAPS
-- =============================================================================
-- Additional keymaps that only apply to standalone Neovim
-- Core shared keymaps are in myconfig/core/keymaps.lua
-- =============================================================================

-- These are already set in core/keymaps.lua when not in VSCode
-- This file is for any additional standalone-specific keymaps

-- Plugin-specific keymaps are defined in their respective plugin files:
-- - Telescope keymaps → plugins/telescope.lua
-- - LSP keymaps → plugins/lsp.lua
-- - Oil keymaps → plugins/oil.lua
-- - etc.

-- You can add any extra standalone keymaps here as needed
</file>

<file path="lua/myconfig/standalone/options.lua">
-- ~\AppData\Local\nvim\lua\myconfig\standalone\options.lua
-- =============================================================================
-- STANDALONE-ONLY OPTIONS
-- =============================================================================
-- Additional options that only apply to standalone Neovim
-- Core options are in myconfig/core/options.lua
-- =============================================================================

-- These are already set in core/options.lua when not in VSCode,
-- but we can add any additional standalone-specific options here

-- Example: Neovim-specific GUI options
if vim.fn.exists('g:neovide') == 1 then
  -- Neovide-specific settings
  vim.g.neovide_cursor_animation_length = 0.05
  vim.g.neovide_cursor_trail_length = 0.8
end

-- Any other standalone-specific options can go here
</file>

<file path="lua/myconfig/vscode/init.lua">
-- ~\AppData\Local\nvim\lua\myconfig\vscode\init.lua
-- =============================================================================
-- VSCODE MODE CONFIGURATION
-- =============================================================================
-- Minimal configuration for VSCode-Neovim integration
-- VSCode handles: LSP, completion, file explorer, fuzzy finding
-- Neovim handles: Modal editing, text objects, motions
-- =============================================================================

-- Load shared core configuration
require("myconfig.core.options")
require("myconfig.core.keymaps")

-- Load VSCode-specific keymaps
require("myconfig.vscode.keymaps")

-- Load VSCode-compatible plugins (if using lazy.nvim)
-- These plugins enhance Neovim's text editing without conflicting with VSCode
require("myconfig.vscode.plugins")

-- =============================================================================
-- VSCODE-SPECIFIC SETTINGS
-- =============================================================================

-- Notify user that VSCode mode is active
vim.notify("Neovim loaded in VSCode mode", vim.log.levels.INFO)

-- =============================================================================
-- NOTES
-- =============================================================================
-- Plugins that work well in VSCode:
-- - flash.nvim (enhanced navigation)
-- - nvim-surround (surround text objects)
-- - vim-commentary or Comment.nvim (commenting)
-- - nvim-treesitter (better syntax understanding)
-- - targets.vim (additional text objects)
--
-- Plugins to AVOID in VSCode:
-- - nvim-cmp, blink.cmp (VSCode has its own completion)
-- - telescope.nvim (VSCode has Ctrl+P)
-- - oil.nvim, nvim-tree (VSCode has file explorer)
-- - lualine.nvim (VSCode has statusbar)
-- - All LSP plugins (VSCode has LSP)
-- - gitsigns (VSCode has GitLens)
-- =============================================================================
</file>

<file path="lua/myconfig/vscode/keymaps.lua">
-- ~\AppData\Local\nvim\lua\myconfig\vscode\keymaps.lua
-- =============================================================================
-- VSCODE-SPECIFIC KEYMAPS
-- =============================================================================
-- These keymaps call VSCode commands through the vscode-neovim extension
-- They integrate Neovim's modal editing with VSCode's features
-- =============================================================================

-- Helper function to call VSCode commands
local vscode = require('vscode')

local keymap = vim.keymap.set

-- =============================================================================
-- FILE NAVIGATION
-- =============================================================================

-- Quick Open (VSCode's fuzzy finder)
keymap("n", "<leader>ff", function() vscode.action("workbench.action.quickOpen") end,
  { desc = "VSCode: Quick Open" })

-- Recent files
keymap("n", "<leader>fr", function() vscode.action("workbench.action.openRecent") end,
  { desc = "VSCode: Recent files" })

-- File explorer
keymap("n", "<leader>e", function() vscode.action("workbench.action.toggleSidebarVisibility") end,
  { desc = "VSCode: Toggle sidebar" })
keymap("n", "-", function() vscode.action("workbench.view.explorer") end,
  { desc = "VSCode: Focus explorer" })

-- =============================================================================
-- SEARCH AND REPLACE
-- =============================================================================

-- Global search
keymap("n", "<leader>fw", function() vscode.action("workbench.action.findInFiles") end,
  { desc = "VSCode: Find in files" })

-- Search in current file
keymap("n", "<leader>fs", function() vscode.action("actions.find") end,
  { desc = "VSCode: Find in file" })

-- Replace in current file
keymap("n", "<leader>sr", function() vscode.action("editor.action.startFindReplaceAction") end,
  { desc = "VSCode: Find and replace" })

-- =============================================================================
-- CODE NAVIGATION (LSP)
-- =============================================================================

-- Go to definition (also works with 'gd' by default)
keymap("n", "<leader>gd", function() vscode.action("editor.action.revealDefinition") end,
  { desc = "VSCode: Go to definition" })

-- Go to references
keymap("n", "<leader>gr", function() vscode.action("references-view.findReferences") end,
  { desc = "VSCode: Find references" })

-- Go to implementation
keymap("n", "<leader>gi", function() vscode.action("editor.action.goToImplementation") end,
  { desc = "VSCode: Go to implementation" })

-- Go to type definition
keymap("n", "<leader>gt", function() vscode.action("editor.action.goToTypeDefinition") end,
  { desc = "VSCode: Go to type definition" })

-- Show hover information
keymap("n", "K", function() vscode.action("editor.action.showHover") end,
  { desc = "VSCode: Show hover" })

-- Peek definition
keymap("n", "<leader>pd", function() vscode.action("editor.action.peekDefinition") end,
  { desc = "VSCode: Peek definition" })

-- =============================================================================
-- CODE ACTIONS
-- =============================================================================

-- Code actions (quick fix)
keymap({ "n", "v" }, "<leader>ca", function() vscode.action("editor.action.quickFix") end,
  { desc = "VSCode: Code actions" })

-- Rename symbol
keymap("n", "<leader>rn", function() vscode.action("editor.action.rename") end,
  { desc = "VSCode: Rename symbol" })

-- Format document
keymap("n", "<leader>fm", function() vscode.action("editor.action.formatDocument") end,
  { desc = "VSCode: Format document" })

-- Format selection
keymap("v", "<leader>fm", function() vscode.action("editor.action.formatSelection") end,
  { desc = "VSCode: Format selection" })

-- =============================================================================
-- DIAGNOSTICS (Problems)
-- =============================================================================

-- Show problems panel
keymap("n", "<leader>xx", function() vscode.action("workbench.actions.view.problems") end,
  { desc = "VSCode: Show problems" })

-- Next diagnostic
keymap("n", "]d", function() vscode.action("editor.action.marker.next") end,
  { desc = "VSCode: Next diagnostic" })

-- Previous diagnostic
keymap("n", "[d", function() vscode.action("editor.action.marker.prev") end,
  { desc = "VSCode: Previous diagnostic" })

-- Show diagnostic (error/warning)
keymap("n", "<leader>d", function() vscode.action("editor.action.showHover") end,
  { desc = "VSCode: Show diagnostic" })

-- =============================================================================
-- EDITOR TABS/SPLITS
-- =============================================================================

-- Split editor
keymap("n", "<leader>sv", function() vscode.action("workbench.action.splitEditorRight") end,
  { desc = "VSCode: Split right" })
keymap("n", "<leader>sh", function() vscode.action("workbench.action.splitEditorDown") end,
  { desc = "VSCode: Split down" })

-- Close editor
keymap("n", "<leader>c", function() vscode.action("workbench.action.closeActiveEditor") end,
  { desc = "VSCode: Close editor" })

-- Navigate between editor groups
keymap("n", "<C-h>", function() vscode.action("workbench.action.navigateLeft") end,
  { desc = "VSCode: Navigate left" })
keymap("n", "<C-l>", function() vscode.action("workbench.action.navigateRight") end,
  { desc = "VSCode: Navigate right" })
keymap("n", "<C-k>", function() vscode.action("workbench.action.navigateUp") end,
  { desc = "VSCode: Navigate up" })
keymap("n", "<C-j>", function() vscode.action("workbench.action.navigateDown") end,
  { desc = "VSCode: Navigate down" })

-- Navigate between tabs
keymap("n", "<S-h>", function() vscode.action("workbench.action.previousEditor") end,
  { desc = "VSCode: Previous tab" })
keymap("n", "<S-l>", function() vscode.action("workbench.action.nextEditor") end,
  { desc = "VSCode: Next tab" })

-- =============================================================================
-- GIT INTEGRATION
-- =============================================================================

-- Git status
keymap("n", "<leader>gs", function() vscode.action("workbench.view.scm") end,
  { desc = "VSCode: Git status" })

-- Git blame
keymap("n", "<leader>gb", function() vscode.action("gitlens.toggleLineBlame") end,
  { desc = "VSCode: Toggle git blame" })

-- Git diff
keymap("n", "<leader>gd", function() vscode.action("workbench.action.compareEditor.focusPrimarySide") end,
  { desc = "VSCode: Git diff" })

-- Next/Previous git change
keymap("n", "]c", function() vscode.action("workbench.action.editor.nextChange") end,
  { desc = "VSCode: Next git change" })
keymap("n", "[c", function() vscode.action("workbench.action.editor.previousChange") end,
  { desc = "VSCode: Previous git change" })

-- =============================================================================
-- COMMENTING
-- =============================================================================

-- Toggle line comment (also 'gcc' works with vim-commentary)
keymap("n", "<leader>/", function() vscode.action("editor.action.commentLine") end,
  { desc = "VSCode: Toggle comment" })
keymap("v", "<leader>/", function() vscode.action("editor.action.commentLine") end,
  { desc = "VSCode: Toggle comment" })

-- =============================================================================
-- WORKSPACE/PROJECT
-- =============================================================================

-- Command palette
keymap("n", "<leader>p", function() vscode.action("workbench.action.showCommands") end,
  { desc = "VSCode: Command palette" })

-- Save file
keymap("n", "<leader>w", function() vscode.action("workbench.action.files.save") end,
  { desc = "VSCode: Save file" })

-- Save all
keymap("n", "<leader>wa", function() vscode.action("workbench.action.files.saveAll") end,
  { desc = "VSCode: Save all" })

-- Toggle terminal
keymap("n", "<leader>t", function() vscode.action("workbench.action.terminal.toggleTerminal") end,
  { desc = "VSCode: Toggle terminal" })

-- Focus on terminal
keymap("n", "<leader>tt", function() vscode.action("workbench.action.terminal.focus") end,
  { desc = "VSCode: Focus terminal" })

-- =============================================================================
-- DEBUGGING
-- =============================================================================

-- Toggle breakpoint
keymap("n", "<leader>db", function() vscode.action("editor.debug.action.toggleBreakpoint") end,
  { desc = "VSCode: Toggle breakpoint" })

-- Start debugging
keymap("n", "<leader>dc", function() vscode.action("workbench.action.debug.start") end,
  { desc = "VSCode: Start debugging" })

-- =============================================================================
-- FOLD/UNFOLD
-- =============================================================================

-- Toggle fold at cursor
keymap("n", "za", function() vscode.action("editor.toggleFold") end,
  { desc = "VSCode: Toggle fold" })

-- Fold all
keymap("n", "zM", function() vscode.action("editor.foldAll") end,
  { desc = "VSCode: Fold all" })

-- Unfold all
keymap("n", "zR", function() vscode.action("editor.unfoldAll") end,
  { desc = "VSCode: Unfold all" })

-- =============================================================================
-- NOTES
-- =============================================================================
-- To discover VSCode command IDs:
-- 1. Open VSCode Command Palette (Ctrl+Shift+P)
-- 2. Run: "Developer: Open Keyboard Shortcuts (JSON)"
-- 3. Or check: https://code.visualstudio.com/api/references/commands
--
-- The vscode.action() function calls VSCode commands from Neovim
-- You can chain commands and even pass arguments if needed
-- =============================================================================
</file>

<file path="lua/myconfig/vscode/plugins.lua">
-- ~\AppData\Local\nvim\lua\myconfig\vscode\plugins.lua
-- =============================================================================
-- VSCODE-COMPATIBLE PLUGINS
-- =============================================================================
-- Only load plugins that enhance text editing without conflicting with VSCode
-- VSCode handles LSP, completion, file explorer, fuzzy finding, git decorations
-- =============================================================================

-- Bootstrap lazy.nvim (same as standalone)
local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
if not vim.loop.fs_stat(lazypath) then
  vim.fn.system({
    "git", "clone", "--filter=blob:none",
    "https://github.com/folke/lazy.nvim.git",
    "--branch=stable",
    lazypath,
  })
end
vim.opt.rtp:prepend(lazypath)

-- Load plugins
require("lazy").setup({
  
  -- ==========================================================================
  -- ENHANCED NAVIGATION
  -- ==========================================================================
  
  -- Flash: Enhanced f/t motions and search
  {
    "folke/flash.nvim",
    event = "VeryLazy",
    opts = {},
    keys = {
      { "s", mode = { "n", "x", "o" }, function() require("flash").jump() end, desc = "Flash" },
      { "S", mode = { "n", "x", "o" }, function() require("flash").treesitter() end, desc = "Flash Treesitter" },
      { "r", mode = "o", function() require("flash").remote() end, desc = "Remote Flash" },
      { "R", mode = { "o", "x" }, function() require("flash").treesitter_search() end, desc = "Treesitter Search" },
    },
  },
  
  -- ==========================================================================
  -- TEXT OBJECTS
  -- ==========================================================================
  
  -- Surround: Add/change/delete surrounding pairs
  {
    "kylechui/nvim-surround",
    version = "*",
    event = "VeryLazy",
    config = function()
      require("nvim-surround").setup({})
    end,
  },
  
  -- Targets: Additional text objects (separators, arguments, etc.)
  {
    "wellle/targets.vim",
    event = "VeryLazy",
  },
  
  -- ==========================================================================
  -- COMMENTING
  -- ==========================================================================
  
  -- Comment.nvim: Smart commenting
  {
    "numToStr/Comment.nvim",
    event = "VeryLazy",
    config = function()
      require("Comment").setup()
    end,
  },
  
  -- ==========================================================================
  -- SYNTAX & TREESITTER (Optional)
  -- ==========================================================================
  
  -- Treesitter: Better syntax understanding for text objects
  -- Note: VSCode has its own syntax highlighting, but treesitter
  -- can still help with text objects and motions
  {
    "nvim-treesitter/nvim-treesitter",
    build = ":TSUpdate",
    event = "VeryLazy",
    dependencies = {
      "nvim-treesitter/nvim-treesitter-textobjects",
    },
    config = function()
      require("nvim-treesitter.configs").setup({
        -- Only install parsers you need
        ensure_installed = {
          "lua", "javascript", "typescript", "python",
          "html", "css", "json", "markdown",
        },
        
        -- Enable only features that work well with VSCode
        highlight = {
          enable = false,  -- VSCode handles highlighting
        },
        
        indent = {
          enable = false,  -- VSCode handles indentation
        },
        
        -- Text objects are useful even in VSCode
        textobjects = {
          select = {
            enable = true,
            lookahead = true,
            keymaps = {
              ["af"] = "@function.outer",
              ["if"] = "@function.inner",
              ["ac"] = "@class.outer",
              ["ic"] = "@class.inner",
            },
          },
        },
      })
    end,
  },
  
  -- ==========================================================================
  -- MULTI-CURSOR (Optional)
  -- ==========================================================================
  
  -- Visual Multi: Multiple cursors (if you prefer Neovim's version)
  -- Note: VSCode has built-in multi-cursor, so this is optional
  {
    "mg979/vim-visual-multi",
    event = "VeryLazy",
    enabled = false,  -- Disabled by default, enable if you prefer it
  },
  
}, {
  -- Lazy.nvim options
  ui = {
    border = "rounded",
  },
  performance = {
    rtp = {
      disabled_plugins = {
        "gzip",
        "matchit",
        "matchparen",
        "netrwPlugin",
        "tarPlugin",
        "tohtml",
        "tutor",
        "zipPlugin",
      },
    },
  },
})

-- =============================================================================
-- NOTES ON VSCODE-COMPATIBLE PLUGINS
-- =============================================================================
--
-- RECOMMENDED (These enhance Neovim without conflicts):
-- ✅ flash.nvim - Enhanced navigation (s/S for jump)
-- ✅ nvim-surround - Surround text objects (ys/cs/ds)
-- ✅ Comment.nvim - Smart commenting (gcc/gc)
-- ✅ targets.vim - Additional text objects (cin'/da,)
-- ✅ nvim-treesitter - Better text objects (only textobjects feature)
--
-- OPTIONAL (Can work but may have quirks):
-- ⚠️ leap.nvim - Alternative to flash.nvim
-- ⚠️ vim-visual-multi - Multi-cursor (VSCode has its own)
-- ⚠️ indent-blankline - Indent guides (VSCode has extensions)
--
-- DO NOT USE (Conflicts with VSCode):
-- ❌ telescope.nvim - VSCode has Ctrl+P
-- ❌ oil.nvim / nvim-tree - VSCode has file explorer
-- ❌ nvim-cmp / blink.cmp - VSCode has IntelliSense
-- ❌ All LSP plugins - VSCode has LSP
-- ❌ lualine.nvim - VSCode has statusbar
-- ❌ gitsigns.nvim - VSCode has GitLens
-- ❌ bufferline.nvim - VSCode manages tabs
-- ❌ which-key.nvim - Can conflict with VSCode shortcuts
-- ❌ toggleterm.nvim - VSCode has integrated terminal
-- =============================================================================
</file>

</files>
